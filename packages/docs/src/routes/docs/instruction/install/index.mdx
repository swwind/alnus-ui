---
title: Installation
---

# Installation

Firstly, install `tailwindcss` yourself.

After that, install `litheui` package.

```bash
yarn add -D litheui
```

Finally, add following code to `tailwind.config.js`:

```js
module.exports = {
  plugins: [require("litheui")],
};
```
